{
  // extend your base config so you don't have to redefine your compilerOptions
  "extends": "./tsconfig.json",
  "include": ["./eslint.config.mjs", "./client/src/**/*"],
  "exclude": ["node_modules"]
}
